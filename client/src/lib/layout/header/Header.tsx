import NavBar from "lib/components/common/navBar/NavBar";

/**
 * This component returns the header which will be shared accross components and pages
 * @returns NavBar
 */
const Header = () => {
  return <NavBar />;
};

export default Header;
